# PR 58 review continuation

Route: Deep continuation of Location Map. Deliverable: assess PR 58, fix confirmed
defects with regression evidence, update the done changelog, commit/push, and
complete a fresh GitHub Codex code/security review plus CI on the final head.
No merge or release. Baseline: `5abd063ea34d336d9c052c23d7dd4c2ce7321af4`;
comparison: `git diff 48ec832a6c9c1e351981b0f3797eee56bc873203...HEAD`.

## Sources and decisions

The original `.scratch/location-map/` tracker is absent in this checkout. Use
the retained implementation, polish, dark-style and release-qualification plans,
`next feature.md`, `CONTEXT.md`, and PR discussion as specification evidence.
Standards/spec assessments stay separate and T0-owned, as required by the repo
SDD agreement (overriding the code-review skill's delegated reviewers).
Existing approved seams remain viewer actions/render, real metadata, geographic
input/output, tile HTTP, Favorite storage and trial launch/evidence. No new
dependencies. Qodana CI is waived; use the documented GoLand fallback, including
weak warnings, and record exact scope and limitations. Full tests run in CI.

## Tasks and acceptance commands

1. T0: assess the six unresolved code/security findings and the PR's standards
   and spec axes. One read-only scout locates entry-path contracts in
   `internal/ui/{shortcuts,visualsearch,locationmap}.go`; no review or edits.
   Verify factual locators using `rg -n` and targeted reads. Budget: one scout.
2. T0, sequential red/green slices: retained-map search admission, extensionless
   trial format accounting, live-themed placeholders, trial predecessor cleanup,
   tile redirect policy, cancellable/bounded donor agreement. Use existing test
   files where possible. Each confirmed defect must first fail for its stated
   reason, then pass the owning focused test with `-tags no_emoji,nodynamic`.
   Exact test names and commands are recorded as evidence below. Preserve the
   all-pairs 100 m donor contract, real image navigation and normal update paths.
3. T0: inspect changed Go files, run focused race regressions, `make fmt-check
   vet build check-test-shards check-qodana-test-exclusions`, and update `todos.md`.
4. T0: commit/push, reply to and resolve addressed threads, obtain another fresh
   code/security review and passing CI/CodeQL on the latest commit. Repeat for
   confirmed new findings. Verify with `gh pr view 58`, full review threads,
   connector summary and latest workflow results. No local broad race suite.

Task graph: 1 -> sequential slices in 2 -> 3 -> 4 (repeat 2-4 if needed).
Budget: one scout, zero delegated implementations/reviews/fixes; review rounds
continue as required by the user. Previously documented native measurement and
composite test gaps remain explicit; headless checks do not prove native paint.

## Delegation gate

Read-only entry-path scout: G1 prompt <=25 lines; G2 returned locators verified
with `rg -n`; G3 three files/no writes; G4 independent call-path tracing while
T0 handles tile/geographic and trial findings; G5 those entry paths are unread.
S: branch/callback relationships require comprehension. W: no implementation
provided or delegated. One T3 Luna/medium, as routed by the retained plan.

## Evidence

Donor-work decision: retain exact 100 m all-pairs acceptance. Collapse identical
GPS coordinates, prove tightly clustered sets using the triangle inequality,
and cap the remaining pair comparisons at 1,000,000, with context checks during
both collection and agreement. On exhaustion return the existing unreadable
outcome without borrowing GPS; never invent a conflict or cache a derived result.
This limits a pathological large group with many distinct near-boundary positions:
its no-GPS representative may remain unmapped. Direct recorded GPS is unaffected.
The pure resolver accepts the scan context; test canceled and budget-exhausted
work at that existing geographic seam as well as unchanged ranking/boundaries.

- Clean worktree on `feature/image-map`; local HEAD matches PR 58.
- All baseline CI/CodeQL checks pass. Six unresolved findings on that head;
  three older threads are resolved with dispositions. Latest review reports
  findings, so earlier clean-review comments do not close this continuation.

## Standards assessment

Two confirmed lifecycle/composition breaches, both fixed:

- `internal/ui/visualsearch.go`: a retained map visit could start ranked search
  while map navigation retained precedence. AGENTS.md requires cross-feature
  decisions in root UI and shared restricted browsing order. `searchReference`
  now rejects the active map session for every search entry, including deferred
  setup. Viewer/menu/shortcut tests cover map, image and cluster visits and search
  admission after leaving the map.
- `internal/ui/locationmap/resolution.go`: agreement ignored cancellation inside
  quadratic donor comparisons, contrary to AGENTS.md's background-work
  cancellation and observable shutdown contract. The scan now supplies its
  context; deduplication, a triangle-inequality proof and a finite comparison
  budget bound agreement. Deterministic cancellation is checked before and during
  work. Replacing the context with Background made both guards fail; restored.

No additional actionable baseline code smell found in the reviewed change.

## Spec assessment

Four further confirmed partial requirements, all fixed:

- Trial startup cleanup contradicted the qualification README: "does not change
  the normal PicFetch session." The pre-app cleanup boundary now excludes both
  trial modes, while ordinary portable cleanup remains admitted. A temporary
  artifact test failed because the map trial removed the artifact, then passed.
- Trial format accounting contradicted the implementation plan's "actual admitted
  count/format mix": a MIME-aware extensionless URI produced an empty format key,
  rejected by the evidence checker. It now records `unknown`. Ordinary Fyne file
  URIs do not detect extensionless image MIME types here; the original report's
  sniffing premise was too broad, but the provider-aware admission case is real.
- The empty/offline checkerboard contradicted the accepted dark-style plan:
  "Follow the app theme." Captured Light/Dark/Light pixels now follow resolved
  colors. The test first saw the two charcoal colors in Light mode.
- Tile redirects exceeded the documented "normal OSM network path" (qualification
  README). The store copies its client and rejects redirects, including same
  origin, cross-host, loopback/private addresses and HTTPS downgrade. Each case
  first sent a second request; now only the original request is sent. Existing
  freshness, retries, cancellation and cache tests still pass.

Totals: Standards 2, Spec 4; all six resolved in this working change. Existing
composite acceptance gaps in the release-qualification record remain gaps,
including repeated-occurrence raw facts and combined source-reconciliation cases.
Native performance acceptance remains the maintainer's existing decision.

## Local verification

- Full Location Map package race tests: PASS, 1.987s.
- Focused root viewer race regressions (`-run
  '^(TestLocationMap|TestFindMoreLikeThis.*)$'`): PASS, 162.623s.
- Startup/launch race regressions (`-run
  '^(TestTrialLaunchPreservesPredecessorArtifacts|TestLaunchArgs.*)$'`):
  PASS, root package 1.103s.
- Final resolver race tests after strengthening/negatively verifying cancellation:
  PASS, 1.562s. Budget refusal, repeated-coordinate admission, tight-group admission,
  ranking, 100 m boundaries and dateline behavior are covered.
- `make fmt-check vet build check-qodana-test-exclusions
  check-test-shards-direct`: PASS; Linux x86_64 host, 706 UI runnables / 3 shards.
  No new top-level UI tests or test files, so manifests need no additions.
- `git diff --check`: PASS. No dependency changes.
- GoLand fallback: `get_file_problems(errorsOnly=false)`, all 12 changed Go files,
  no diagnostics, weak warnings included, no timeouts. Active profile name is not
  exposed; no Qodana profile parity or IDE-local Qodana scan is claimed. The
  installed inspection tool supplies the documented fallback; no automated
  Qodana-dialog control is available. Qodana CI remains `disabled_manually`.
  Inspected scope: `main.go`, `main_test.go`, `internal/ui/{locationtrial.go,
  locationmap_test.go,visualsearch.go,visualsearch_test.go}`, and
  `internal/ui/locationmap/{resolution.go,resolution_test.go,surface.go,tiles.go,
  tiles_test.go,work.go}`. Final resolver test edit inspected again: clear.
  Revision: working tree based on `5abd063ea34d336d9c052c23d7dd4c2ce7321af4`,
  exact Go diff SHA-256
  `7f11a44281f697118c70fd68c80b205ca581aaa0a6b8ed522efd9b2d0158af8a`.
  Prior unchanged-code inspection evidence remains in the release record.
- Baseline PR merge ref has no open code-scanning alerts. Final pushed-head
  Codex/CI/CodeQL evidence is recorded in the PR conversation after this commit;
  no clean final round is inferred from these local checks.

The requested done/changelog entries are in `todos.md`. A short final report
will call out search admission, placeholder appearance, the conservative large
donor-group fallback limit, tile redirect refusal, and remaining evidence gaps.

## Fresh review of 5d2e70e

All 12 CI/CodeQL checks passed. Security completed at 16:43:42 UTC without new
findings. Code review completed at 16:45:55 UTC with three further findings;
this is not a clean final round. T0 continues sequential red/green fixes at the
existing viewer/clipboard, controlled source-version and URI-reader seams:

1. Gate raw clipboard/copy-selection command boundaries while the map is visible.
2. Keep return presentation/tile demand suspended until source validation finishes.
3. Treat unavailable source versions as requiring revalidation of GPS facts.

Files and exact focused commands are recorded with each reproduced failure below.
No new delegation; root owns the now-known clipboard/return contracts and fixes.
Then repeat changed-file inspections, focused race tests, commit/push, thread
dispositions and another fresh review/CI round. REST review/comment lists require
pagination; use GraphQL thread pagination and the connector summary for the final
gate, since default first-page REST responses can omit new findings.

### Standards assessment

- Confirmed clipboard admission inconsistency: raw shortcuts reached hidden-image
  copy commands despite disabled map menus. The image/path command boundaries
  and shared region-copy availability now exclude the visible map, while visible
  map-origin photos retain clipboard commands.
- Confirmed return lifecycle violation: presentation and tile requests preceded
  source validation. The callback now transitions from the retained photo/cluster
  only after validation; changed sources retire old derived points before display.
  Existing generation checks still suppress replacement/closed-session delivery.
  Copy Selection started during the check is yielded before showing the map; a
  busy copy defers presentation but cannot discard discovered source changes.

### Spec assessment

- Confirmed raw-fact freshness gap: equal unknown stat versions do not prove a
  provider image unchanged. An unavailable version now conservatively triggers
  a rebuild, using the existing uncached metadata path for unknown sources.
  The current photo/cluster remains visible while the worker checks source
  versions. This can delay the visual return on slow storage; provider sources
  without versions incur metadata rereads. Manual camera retention is unchanged.

### Red/green evidence

Commands use `go test -tags no_emoji,nodynamic -count=1 ... ./internal/ui`:

- `-run '^TestLocationMap$/clipboard_shortcuts$'`: failed with hidden image copy
  count 1 and active region mode; passed after command guards. Image-copy delivery
  is drained before the path shortcut so a busy copy cannot mask the path guard.
- `-run '^TestLocationMap$/external_changes/return_validation_barrier$'`: direct
  and cluster routes both failed with visible stale map and tile requests 6 -> 12
  while validation delivery was held. Both pass after ordering repair. Idle Copy
  Selection during validation is canceled before the validated map is exposed.
  A held in-flight clipboard dispatch additionally proves that deferred return
  retains discovered source changes; the expanded barrier race test passes in
  3.964s.
- `-run '^TestLocationMap$/external_changes/unversioned_source$'`: after correcting
  the fixture to use the public map image-open action, failed with old latitude
  52.52 rather than 40.7; passed after unknown-version handling.
- Combined new regressions and existing external-change routes: PASS, 0.613s.
  Existing cases include retained camera/hidden donor refresh and stale validation
  after collection replacement. New cases remain subtests of `TestLocationMap`;
  no exact Qodana exclusion or UI shard manifest changes are required.
- GoLand fallback, `get_file_problems(errorsOnly=false)`: all five changed Go files
  clear, including weak warnings; no timeouts. Scope: `internal/ui/{clipboard.go,
  copyselection.go,locationmap.go,locationmap_test.go}` and
  `internal/ui/locationmap/work.go`. Same documented profile limitation as above.
  Inspected revision: working tree based on `5d2e70e`, Go diff SHA-256
  `aa5497bc294fec4dfb97930a3fbdb8c74a1c80d40153d83cf22dd03fedba9791`.
  The final busy-copy addition and existing held-tile test repair were inspected
  again and are clear. The final source-reconciliation ordering fix was inspected
  again and is clear.
  Unchanged code carries forward its earlier inspection evidence.
- `make fmt-check vet build check-qodana-test-exclusions check-test-shards-direct`:
  PASS after the final product fix; 706 UI runnables / 3 shards. Another fresh
  latest-head review and complete CI remain required.

### Focused-run hang diagnosis

The original focused race run was stopped with a goroutine dump after it stalled
in `TestLocationMap/tile_recovery/hidden_direct_visit`; this is an interrupted
gate, not a pass. The user's separate running PicFetch process was untouched.
The minimized race command with `-timeout=8s -run
'^TestLocationMap$/tile_recovery/hidden_direct_visit$'` reproduced the timeout.
Ranked hypotheses were stale test notification/synchronous-return assumption,
broken production tile cancellation, and cross-test clipboard state. Isolation
ruled out cross-test state. Waiting for actual returned-map UI delivery instead
of a buffered old transport notification fixed the test without another product
change; focused race PASS, 1.979s, including closing the restored map and joining
its held tile workers. No test exclusions,
timeouts used as success, debug instrumentation or weakened cancellation checks.

The original focused map/clipboard race rerun completed in 137.901s with one
failure: `automatic_rebuild/frozen_visit_survivors`. The clipboard/copy-selection
tests and other map cases passed. Isolation reproduced last-cluster-member
deletion failing to return (2.092s). Source reconciliation admitted return
validation and then canceled it by rebuilding the source generation. Reordering
those operations fixes the product regression: retire/rebuild first, then admit
the exhausted visit's return. Race coverage for `automatic_rebuild`,
`external_changes` and `tile_recovery` now passes in 17.772s. The final complete
`TestLocationMap` race run passes in 84.085s. Latest commit/review/CI evidence is
recorded in the PR conversation after push; those remote gates are not inferred
from these local passes.

## Fresh review of b906262

All 12 CI checks passed. Fresh code review completed at 17:23:26 UTC with one
new confirmed spec finding: `next feature.md` explicitly requires naming the GPS
source in the preview when a representative borrows another member's location.
The retained `Point.Donor` was not exposed by the polished hover caption. Existing
`selection_feedback/duplicate_provenance` only proves representative highlighting,
not donor disclosure. Security completed at 17:24:53 UTC without new findings;
the code finding means this is not a clean final round.

T0 adds strict mounted-caption assertions to the existing
`duplicate_locations/representative_and_fallback` direct/borrowed cases, verifies red, then
adds the already-translated `Location from %s` caption while keeping the opened
image unchanged. No new seam, dependency, string key or delegated work. Verify the
owning cases first, then the full affected Location Map race test, locale parity,
format/vet/build/manifests and GoLand all-warning inspections of both Go files.
Update Done changelog, push, reply/resolve, and repeat fresh reviews/CI.

Evidence:

- Exact red command: `go test -tags no_emoji,nodynamic -count=1 -run
  '^TestLocationMap$/duplicate_locations/representative_and_fallback$'
  ./internal/ui`. Borrowed case failed with caption `z-representative.jpg`
  instead of the filename plus `Location from a-donor.jpg`.
- Added the donor line only when `Point.Donor.URI` exists. Reused the existing
  English/German translation key. The strict rendered-label test verifies both
  direct and borrowed captions, tooltip dismissal, and representative opening.
  Filename lookup helpers now recognize the filename line in a multiline caption;
  the provenance assertions still require the exact whole caption.
- Focused duplicate/selection/external-change race tests: PASS, 18.220s.
  Root locale parity/identity/no-arrow tests: PASS, 0.023s.
- Captured and visually inspected direct and borrowed tooltips under
  `/tmp/picfetch-provenance.pDnop7`: donor text is readable, the direct caption
  remains single-line, and the photo remains visible. These are headless Fyne
  captures, not native-platform paint qualification or committed golden changes.
- GoLand fallback inspected `internal/ui/locationmap/surface.go` and
  `internal/ui/locationmap_test.go` with weak warnings included: no diagnostics or
  timeouts. Revision: working tree based on `b906262`, Go diff SHA-256
  `94a6d0e37876867bfa8f4fcce96c717dee3d32e2775996727c4568bb7446dc28`.
  Other code carries forward unchanged-code inspection evidence.
- Full `TestLocationMap` race: PASS, 137.946s. `make fmt-check vet build
  check-qodana-test-exclusions check-test-shards-direct`: PASS; 706 UI runnables /
  3 shards. `git diff --check`: PASS. Fresh code/security/CI gates will follow;
  final latest-head evidence is recorded in the PR conversation after push.

## Fresh review of 5892dbe

All 12 CI checks passed. Code review completed at 17:41:02 UTC with two further
findings; security completed at 17:46:41 UTC without additional findings.
Confirmed standards issue: ranked Grid
restoration intentionally does not close its overlay, so map entry must explicitly
end ranked presentation after retiring visual search. Preserve the existing
ordinary Grid visit/selection behavior. Confirmed memory-bound issue: tile entries
retain full HTTP response headers and unbounded validators outside payload budgets.

T0 fixes sequentially at existing viewer/search and TileStore HTTP seams. First
add map-entry regressions from live ranked results (menu and Shift+L), observe red,
then close only ranked Grid on entry. Next reproduce oversized/unrelated HTTP
metadata retention and implement conservative bounded freshness/validator retention
for both 200 and 304 responses. Preserve ordinary freshness, no-store/no-cache,
conditional requests and enforce the configured budgets; exact policy follows.
No new delegation, packages or dependencies. Finish with changed/focused race
tests, static inspections, repository checks, Done changelog, push and fresh reviews.

Policy and evidence:

- Tile retention keeps only Cache-Control, Expires, Date, Age, ETag and
  Last-Modified. Selected metadata is capped at 4 KiB per entry, including after
  a 304 merge. Values are copied, and each value's data/name plus a 16-byte string
  header is charged; repeated empty values are therefore bounded as well. Unknown
  response headers are not cloned or retained. Oversized selected metadata or
  metadata larger than the configured encoded budget makes that response
  uncacheable, but successfully decoded pixels still reach the viewport.
- `Usage`/`TileUsage` encoded bytes now include retained response metadata, even
  for decoded-only entries. Eviction first frees encoded pixels, then decoded-only
  entries if needed to keep retained metadata within the encoded budget. A 304
  growth updates the charge and trims before returning. This is cache accounting,
  not an exact total-process RSS bound; normal fixed Go object overhead remains.
  The budget can hold slightly fewer tiles; oversized-cache-header responses may
  need to be fetched again. Earlier native encoded-byte observations predate this
  accounting change and retain their original revision attribution.
- Ranked entry red: both menu and Shift+L produced map=true/grid=true/ranked=true
  after search had closed. Green race command `-run
  '^TestLocationMap$/(ranked_search_entry|scope_and_entry)$'`: PASS, 5.493s, including
  ordinary selection preservation and normal full-collection Grid after map exit.
- `TestTileStoreResponseMetadata` red: all six 200/304 oversized-field cases
  retained cache state, unrelated-header case reported only pixel bytes, metadata
  did not cause eviction (2 requests instead of 3), and 304 growth retained an
  over-budget encoded entry. Green coverage additionally checks decoded-only
  eviction and aggregate metadata limits after merging a 304.
- Full `./internal/ui/locationmap` race after consolidating eviction: PASS,
  2.352s. Existing freshness/no-store/no-cache/conditional-request/cancellation
  coverage passes. Two old exact Usage assertions were updated to include their
  known retained header charges; byte limits were not relaxed.
- GoLand fallback inspected all five changed Go files, including weak warnings:
  no diagnostics/timeouts. Scope: `internal/ui/{locationmap.go,locationmap_test.go}`
  and `internal/ui/locationmap/{tiles.go,tiles_test.go,viewport.go}`. Revision:
  working tree based on `5892dbe`, Go diff SHA-256
  `d1fd98e018a549faabc02e33440e8d243899cc9fb3b38071b69c4268e8f25553`.
  Unchanged code carries forward earlier inspection evidence; Qodana CI remains
  explicitly waived. No new test files or top-level root-UI tests were added.
- Affected map/search race (`-run '^(TestLocationMap|TestFindMoreLikeThis.*)$'`):
  PASS, 195.751s. This run finished as the maintainer requested faster iteration:
  from here, run only individual/focused regressions locally and leave broad
  suites to CI. `make fmt-check vet build check-qodana-test-exclusions
  check-test-shards-direct`: PASS, 706 UI runnables / 3 shards.
  `git diff --check`: PASS. Another fresh latest-head remote round is required.

## Final requested batch: review of 396475d

All 12 CI checks passed. Fresh security completed at 18:10:56 UTC without new
findings; code review completed at 18:13:39 UTC with five further findings.
The maintainer requested stopping after this fix batch: commit/push its fixes
and evidence, then hand off without waiting for another remote round. Automatic
reviews/CI may continue; do not claim the loop has reached a clean final gate.

T0 owns these sequential SDD/TDD slices at the existing viewer-input, raw GPS
cache, Favorite filesystem and native evidence seams. No new delegation:

1. Validate D/Shift+D throughout active map visits; test map/direct image/cluster
   Grid/cluster image plus restoration after leaving, using a new subtest of
   `TestLocationMap` (focused `duplicate_shortcuts` only). Assessment: false
   positive. Existing `toggleHideDuplicates`/`browseCurrentDuplicates` reject the
   restricted browsing context and `grid.HandleKey` rejects D for subsets.
   All eight cases pass without a production change. Retain the regression test.
2. Project retained map metadata to GPS scalars on memory/disk/read boundaries;
   verify large unrelated EXIF strings do not survive while GPS remains correct
   (`TestFactCacheRetainsOnlyLocation`, focused Favorite cache regressions).
3. Bound retired Favorite cleanup by entries and batches, check cancellation
   between filesystem operations, and avoid recursive deletion of unexpected
   directory trees. Verify cancellation and over-budget leftovers at the Favorite
   storage seam, without timers or real desktop operations.
4. Bind native cancellation/close evidence to the stable closed-viewer screen
   captured before opening, not arbitrary changed map pixels. Native evidence
   must fail closed when the target cannot be identified. Run focused protocol
   regressions locally; macOS Swift checks belong to CI (no Swift toolchain here).
5. Correct the package-main cleanup boundary in ARCHITECTURE.md. Update Done,
   inspect changed code including weak warnings, run only targeted local tests,
   commit/push, reply/resolve the addressed threads, and stop as requested.

Batch evidence and limits:

- Duplicate-shortcut finding rejected: `TestLocationMap/duplicate_shortcuts`
  covers plain/shifted D on the map, direct image, cluster Grid and cluster image,
  plus normal D after exit. All eight passed on unchanged production code
  (0.938s verbose run); no redundant dispatcher guard was added.
- `TestFactCacheRetainsOnlyLocation` failed because non-location EXIF survived.
  Cache publication, source reads, Favorite reads and writes now keep only the
  three GPS scalars. This also prevents unrelated camera strings from making a
  valid GPS disk record exceed its size limit. GPS presence/absence and exact
  coordinates remain unchanged; general EXIF display is untouched.
  `go test -race -tags no_emoji,nodynamic -count=1 -run '^TestFactCache'
  ./internal/ui/locationmap`: PASS, 1.031s.
- `TestFavoriteRetiredCleanup` initially failed all three cases: cancellation
  ignored, all 1,100 records removed in one pass, and an unexpected nested tree
  recursively deleted. Cleanup now examines at most 1,024 namespace/record entries
  per Favorite opening, reads batches of at most 64, checks context before each
  batch/entry/removal, and removes only recognized flat records/temp files and
  empty namespaces. Excess/unrecognized contents remain for later maintenance;
  namespace isolation does not depend on cleanup completion. A blocked individual
  filesystem syscall is still not interruptible. Added mid-removal cancellation
  and legacy Favorite metadata projection coverage.
  `go test -race -tags no_emoji,nodynamic -count=1 -run
  '^(TestFavoriteRetiredCleanup|TestFavoriteFactsRetainOnlyLocation)$'
  ./internal/ui/locationmap`: PASS, 1.153s.
- Native protocol red: both close and cancel accepted an unrelated frame's
  timestamp. The Swift helper now captures a stable closed-viewer body hash before
  entry and accepts exit feedback only on a matching observed frame; protocol
  validation requires that identification. Pan/zoom retain their change rule.
  Animated/changing viewer baselines may now fail qualification instead of
  producing a false latency pass. App state still does not supply pixel timestamps.
  `go test -race -count=1 -run '^TestNativeProtocol'
  ./scripts/locationmapqualify`: PASS, 1.012s. Pure Swift policy cases and production
  warnings-as-errors type-check are added to both macOS CI jobs. No local Swift
  toolchain or native screen session is available: those checks and actual native
  paint/timing remain unverified at this handoff. Earlier samples are not
  retroactively qualified by this change.
- Targeted viewer duplicate-shortcut/Favorite lifecycle race: PASS, 10.743s.
  Separate `TestLocationMap/duplicate_locations/representative_and_fallback`
  race: PASS, 4.583s. No broad local test suite, build or vet was repeated; CI owns
  heavy verification as requested. `make fmt-check check-qodana-test-exclusions
  check-test-shards-direct`: PASS, 706 root-UI runnables / 3 shards.
  Added the exact new `favorites_test.go` Qodana duplication exclusion.
- GoLand fallback (`errorsOnly=false`) inspected all eight changed Go files,
  both Swift files, `.github/workflows/ci.yml` and `qodana.yaml`: no diagnostics or
  timeouts. Weak warnings included; profile identity and Swift language coverage
  are not exposed, so this does not substitute for macOS Swift compilation.
  Scope is the staged code/config diff based on `396475d`, SHA-256
  `185596da446fd2fe15d938faecc29d8b3614f85e2d9d6d5a0b60524e7632a4a9`.
  Unchanged code carries forward prior evidence. Qodana CI remains waived.
- ARCHITECTURE.md now names both isolated trial modes and the updated retention,
  cleanup and observer boundaries. Done changelog is current. The final push and
  five thread dispositions will be recorded on the PR; latest-head CI and fresh
  code/security review remain pending when stopping at the maintainer's request.

## Resumed review of a5b250d

The maintainer renewed the complete review-loop request after the prior stopping
point. Baseline `a5b250db6e356f7452576622a1de8fcb04cb878b`; full PR comparison
remains `48ec832a6c9c1e351981b0f3797eee56bc873203...HEAD`. Three new unresolved
code findings and the failed Linux ui-1 shard require assessment. Security and
CodeQL completed; Qodana CI remains waived. Only individual tests run locally;
GitHub owns the complete suite, build and vet.

T0 owns review and sequential fixes at the previously approved viewer/render,
Favorite-storage and native-observation seams. No dependencies or new seams.

1. Correct the stale full-EXIF assertion after reproducing
   `go test -tags no_emoji,nodynamic -count=1 -run
   '^TestLocationMap$/metadata_coverage/shared_formats/png.png$' ./internal/ui`.
   Keep explicit GPS expectations and source-preservation checks.
2. Bound Favorite ownership to the supplied live source scope, including scan,
   saved-fact promotion and committed invalidation. TDD at the existing Favorite
   storage seam: `TestFavoriteFactsLiveScope`; retain raw full-list namespace
   identity and exact saved-member authority. Irrelevant Favorites must not get
   map-cache directories or retained handles. Files: favorites.go, work.go and
   favorites_test.go. Verify each affected Favorite test individually.
3. TDD initial partial tile visibility at the viewer's HTTP/render seam; retain
   complete-scene replacement for already-painted maps per the accepted polish
   specification. Files: viewport.go and locationmap_test.go. Verify the new
   `TestLocationMap/tile_recovery/initial_partial` case and existing atomic
   replacement cases individually.
4. Validate and correct native pan/zoom observation correlation. Files: existing
   native helper, its policy fixture and Go protocol tests as required. Single
   `TestNativeProtocol` plus macOS CI observer checks prove the protocol; native
   screen timing remains separate evidence, never inferred from unit tests.
5. Inspect every changed code file including weak warnings, update Done/evidence,
   commit/push, reply/resolve threads, and wait for fresh code/security reviews
   and passing required CI/CodeQL at that head. Repeat confirmed findings.

Graph: individual red/green slices 1 -> 2 -> 3 -> 4 -> 5. One read-only T3 scout
maps native inputs/fixtures while T0 handles other findings; no delegated review
or implementation. G1 bounded prompt; G2 locators verified by targeted reads;
G3 no writes; G4/G5 independent unread native protocol context; S relationships
need comprehension, W no implementation delegated. Budget: one scout, zero fix
delegates, as many remote review rounds as completion requires.

### Assessment and local evidence

Standards: the Favorite inventory retained unrelated saved membership and handles;
the source-scoped inventory now covers scan, promotion and committed invalidation.
Spec: initial partial tile success was hidden unnecessarily. The accepted polish
contract still requires atomic replacement once pixels are painted; initial
successes now mount independently, and retries fill their existing placements.
The failed CI assertion compared unrelated EXIF fields with deliberately projected
GPS-only facts. It now compares GPS presence and coordinates and still verifies
explicit-zero metadata and unchanged source bytes.

Native timing: confirmed that hash changes do not identify requested transforms;
also found pan commands lacked Shift after arrows became photo selection. The
chosen containment rejects unverified pan/zoom samples in the Swift helper, Go
protocol and persisted report validator. Formal automatic latency qualification
is unavailable until independent visual correlation is implemented; this open
work is in `todos.md`. The smaller scope was presented as an optional choice and
used after the response window, consistent with the existing release performance
waiver. It is not a measured latency pass or a claim that correlation is solved.
The protocol now uses Shift+arrows when a qualified driver is available. Manual
trials and ordinary app controls are unchanged. No new dependencies.

Red/green commands use `go test -count=1 -run '^NAME$' PACKAGE`, with
`-tags no_emoji,nodynamic` for imaging/viewer packages and `-race` for the green
regressions (the reproduced PNG metadata test's green run did not use race):

- `TestLocationMap/metadata_coverage/shared_formats/png.png`, `./internal/ui`:
  red reproduced CI's non-GPS date mismatch; green PASS, 0.165s.
- `TestFavoriteFactsLiveScope`, `./internal/ui/locationmap`: red retained three
  owners/two paths for one live source; green PASS, 1.082s. Two matching Favorites,
  repeated live occurrences, an irrelevant Favorite and empty scope are covered.
- `TestFavoriteRetiredCleanup`: PASS, 1.180s; `TestFavoriteFactsRetainOnlyLocation`:
  PASS, 1.113s, each run individually in the feature package.
- `TestLocationMap/tile_recovery/initial_partial`: red had no mounted successful
  tile; green PASS, 3.024s before the final empty-scene refinement (rerun below).
- `TestLocationMap/tile_recovery/atomic_retry`: PASS, 3.447s;
  `TestLocationMap/tile_recovery/retry_and_restore`: PASS, 4.275s.
- `TestLocationMap/automatic_rebuild/inactive_write_invalidates_persistence`:
  PASS, 5.394s; `TestLocationMap/gps_cache_lifecycle/held_replacement`: PASS,
  4.269s. Each viewer child was a separate command.
- `TestNativeProtocolRejectsUnidentifiedGesture`, `./scripts/locationmapqualify`:
  red accepted both ambiguous samples; green PASS, 1.016s.
- `TestNativeProtocolPanUsesShift` and `TestCheckReportRejectsUnidentifiedGestures`
  were negatively verified against the original permissive/unshifted behavior;
  both failed for the intended reason, then passed restored (1.020s / 1.023s).
- `TestNativeProtocolUsesObservedCountAndRetainsFailedSample`: PASS, 1.022s;
  `TestCheckReportQualifiesTenThousandAtBoundary`: PASS, 1.027s. Synthetic checker
  observations do not establish actual native gesture recognition.

GoLand fallback inspected all ten changed Go files and both Swift files with
`get_file_problems(errorsOnly=false)`: no diagnostics or timeouts, weak warnings
included. Scope: `internal/ui/locationmap_test.go`, locationmap
`{favorites.go,favorites_test.go,viewport.go,work.go}`, qualification
`{evidence.go,evidence_test.go,protocol.go,runner.go,runner_test.go}`, and native
`{capture.swift,capture_test.swift}`. Revision: working tree based on `a5b250d`,
Go/Swift diff SHA-256
`f3a3f03fd6f0d33090060c146e754ee4051f66287e2128079d309b5006677642`.
Profile identity/Swift language coverage are not exposed; macOS CI must compile
and run the Swift policy checks. Unchanged code carries prior inspection evidence.
No local full suite/build/vet; GitHub owns heavy checks as requested. No new test
files or top-level root UI tests. Done changelog and architecture are updated.
Final individual race reruns: initial partial tile display PASS (2.342s), WebP
metadata parity PASS (2.202s). `make fmt-check check-qodana-test-exclusions
check-test-shards-direct` PASS: 706 UI runnables / three shards. `git diff --check`
PASS. Final remote evidence will be posted on the PR after commit/push so recording it
does not create another unreviewed head.

## Fresh review of c34fa11

All 12 CI/CodeQL checks passed, including both macOS observer checks. Security
completed at 19:44:10 UTC without new findings. Code review completed at 19:47:21
UTC with one cache-persistence ordering finding. T0 continues at the existing
Favorite-storage seam; no delegation or new dependency.

Acceptance: a delayed committed-write cleanup must preserve/reconcile a valid
new raw fact, including when its publication beat cleanup to the persistence
mutex. Without a new raw fact it must still remove the invalidated disk record.
Test: `go test -race -tags no_emoji,nodynamic -count=1 -run
'^TestFavoriteInvalidationKeepsNewerFact$' ./internal/ui/locationmap`.
Reconcile against current raw authority and the source version while holding
the existing persistence mutex. This avoids waiting for filesystem work on UI
and makes either worker ordering safe. Verify the existing inactive-write viewer
test individually; inspect changed files, update Done, push and repeat reviews.

Evidence: the deterministic Favorite-storage regression reproduced removal both
when the fresh raw fact was pending publication and when it was already on disk.
The no-new-fact case passed before and after. After reconciliation, the individual
race test PASS (1.082s); the separate inactive-write viewer race test PASS (1.911s).
Cleanup removes invalidated disk state first, then rechecks the current source
version/raw fact and restores that valid fact under the same persistence mutex.
Failed persistence therefore does not intentionally preserve an obsolete record.
The current app UI does not wait for cleanup; fresh source stats stay on workers.

GoLand all-warning fallback inspected `favorites.go`, `favorites_test.go` and
`feature.go`: no diagnostics or timeouts. Revision: working tree based on
`c34fa11`; Go diff SHA-256
`d69821eb06b250125bdc371cda162217ce1abc71822d0f92baa3a7298655d468`.
Other code carries forward the preceding complete inspection and CI evidence.
Done changelog updated. Full suite/build/vet remain assigned to GitHub.
Formatting, exact Qodana test exclusions and the 706-test UI shard inventory pass;
`git diff --check` is clear. Another fresh remote review/CI round is required.

## 2026-09-26 renewed review

Baseline: `c02f6edba7dd8f5c498f578601ee4acea7468582`; PR merge-base remains
`48ec832a6c9c1e351981b0f3797eee56bc873203`. No unresolved threads. Latest
code/security reports on this baseline are clean; all twelve CI/CodeQL checks
pass and the PR merge ref has no open code-scanning alerts. Qodana remains
`disabled_manually`, explicitly waived. No merge or release is authorized.

Continue the existing Deep SDD record at the approved viewer-input/render seam.
T0 owns standards/spec assessment and every fix. One read-only T3 scout mapped
existing lifecycle/return/camera cases while T0 assessed implementation. G1:
bounded standalone prompt; G2: verify locators with targeted reads; G3: no edits;
G4/G5: independent test-path context; S/W: relationship tracing, no implementation.
Budget: one scout, zero implementation delegates; only individual local tests.

Finding to reproduce: map-origin image commands reach map navigation before Copy
Selection input ownership. Escape must first cancel an idle selection and keep
the photo open; G may leave an idle selection but must retire it before showing
Grid. A pending copy owns both keys and must keep the image visit unchanged.
These are existing Copy Selection contracts, not a new browsing design.

Task: T0, `internal/ui/{keys.go,locationmap_test.go}`, then this evidence record
and `todos.md`. Red/green at `TestLocationMap/copy_selection_navigation`, with
direct and cluster-origin image visits and controlled clipboard completion.
Verify with `go test -tags no_emoji,nodynamic -count=1 -run
'^TestLocationMap$/^copy_selection_navigation$' ./internal/ui`; run the green
case with `-race`, then individual affected return/lifecycle regressions.
Inspect each changed Go file including weak warnings; check formatting and
inventory; commit/push the fix and Done changelog. Full suites/build/vet belong
to GitHub. Obtain fresh code/security reviews and CI/CodeQL on the resulting
head, repeating confirmed fixes. Existing performance/composite limits remain.

Graph: reproduce -> minimal dispatcher fix -> local checks/inspections ->
changelog/evidence commit and push -> fresh remote review and CI.
No dependency, new source file, package, translation or test seam is planned.

### Assessment and evidence

Standards: one confirmed input-ownership defect. The root dispatcher sent
map-origin image keys to the map before Copy Selection, contradicting the
existing mode contract. Spec: the same defect breaks ordinary image commands
within the Location Map visit; no separate feature-scope change was needed.
Move the existing Copy Selection consume/yield block before map navigation.
No new controller, callback, worker or mutable seam is introduced.

Red: the individual `copy_selection_navigation` test failed seven of eight
direct/cluster, Escape/G, idle/busy combinations for early navigation or an
active selection left on Grid. The first green race attempt exposed a fixture
cleanup race: releasing the held clipboard without awaiting UI completion let
the inline Fyne test queue race harness teardown. The fixture now releases and
waits on the existing clipboard completion before cleanup; production lifetime
code is unchanged. Final individual race evidence (`./internal/ui`, all with
`-race -tags no_emoji,nodynamic -count=1`):

- `^TestLocationMap$/^copy_selection_navigation$`: PASS, 9.187s.
- `^TestLocationMap$/^external_changes$/^return_validation_barrier$`: PASS, 4.491s.
- `^TestCopySelectionKeyboard$`: PASS, 4.084s.
- `^TestLocationMap$/^lifecycle$/^base_round_trip$`: PASS, 3.666s.
- `^TestVisualSimilarityExplorer$/^cohort_escape_preserves_hide$`: PASS, 2.396s,
  checking the other navigation handler sharing the reordered dispatch.

The lead verified the scout locators: `base_round_trip` already replaces a
collection during an active map-image visit, contrary to the scout's final gap
claim. No redundant test or open issue was added for that covered scenario.

GoLand fallback inspected both changed Go files with
`get_file_problems(errorsOnly=false)`, including weak warnings: no findings or
timeouts. Reinspected the test after fixing its completion barrier. Scope:
`internal/ui/keys.go`, `internal/ui/locationmap_test.go`; revision: working tree
based on `c02f6ed`, Go diff SHA-256
`eb7c66ac6af467d18c0ccd59d780618c86a64747fb8cc7bb9f45f94d2ceddc4d`.
The IDE tool does not expose profile identity; Qodana parity is not claimed.
Unchanged files carry forward the preceding inspection evidence and CI results.
Done changelog updated, including correction of the stale paused-review status.
`make fmt-check check-qodana-test-exclusions check-test-shards-direct`: PASS,
706 root-UI runnables across three shards. No test files or top-level tests were
added. `git diff --check`: PASS. No full local suite, build or vet was run.
Latest-head remote completion evidence will be posted on the PR to avoid
creating an unreviewed documentation commit after the final clean round.

## Fresh review of 7a161a2

All twelve CI/CodeQL checks passed. Security completed at 11:30:03 UTC without
new actionable findings. Code review completed at 11:33:57 UTC with three P2
reports. T0 owns the sequential assessment/fixes; no new delegation.

1. Favorite invalidation: confirm terminal cancellation at the existing storage
   seam. Ordinary map close must retain committed cleanup; terminal Stop may
   abandon cache maintenance. Use the feature lifetime context and check it
   during batched Favorite enumeration and per-source cleanup. A filesystem
   syscall already blocked in the OS remains non-interruptible. Source-version
   validation still rejects stale records on later visits. Files: existing
   `locationmap/{favorites.go,favorites_test.go,feature.go}`. Single commands:
   `TestFavoriteInvalidationCancellation`, `TestFavoriteInventoryCancellation`,
   `TestFavoriteInvalidationKeepsNewerFact`, and the viewer's existing
   `TestLocationMap/automatic_rebuild/inactive_write_invalidates_persistence`.
2. Initial tile baseline: assess against `movePaintedTiles`, which unconditionally
   records every view before fetching/mounting. Run the existing
   `TestLocationMap/tile_recovery/stale_during_drag`, negatively verify its
   first-pan guard by disabling initial baseline recording, then restore it.
   Reject if the reported zero-baseline path cannot occur; no redundant fix.
3. Progressive auto-fit: preserve immediate counts/cards but coalesce automatic
   camera fitting to at most one update per 250 ms, plus immediate initial and
   final fits. Manual camera input remains immediate and cancels the automatic
   adjustment. Use one cancellable, tracked timer and the captured UI queue;
   queued completions recheck the session and manual-camera state. Verify HTTP
   demand and final visibility through a single viewer regression with fake
   time and held source reads, then focused lifecycle/tile cases individually.

Update Done/evidence, inspect changed Go files including weak warnings, commit
and push; reply/resolve all three reports with evidence and complete another
fresh code/security review plus CI. No full local suite/build/vet.

### Assessment and evidence

- Shutdown finding confirmed. `TestFavoriteInvalidationCancellation` reproduced
  maintenance after terminal lifetime cancellation, then passed with `-race`
  (1.039s). `TestFavoriteInventoryCancellation` reproduced the uncancellable
  whole-directory enumeration, then passed (1.046s). The inventory now reads
  batches of 64 and checks cancellation per entry; invalidation checks the feature
  lifetime before inventory and each source/owner. Partial inventories still
  close all retained handles. Ordinary-close cleanup and new-fact reconciliation
  remain intact: individual race `TestFavoriteInvalidationKeepsNewerFact` PASS
  (1.089s), viewer `automatic_rebuild/inactive_write_invalidates_persistence`
  PASS (2.124s), `TestFavoriteFactsLiveScope` PASS (1.068s), and
  `TestFavoriteRetiredCleanup` PASS (1.195s). Active filesystem I/O cannot be
  interrupted; terminal shutdown may abandon remaining cache maintenance.
- Initial tile-baseline finding rejected. `updateTiles` calls `movePaintedTiles`
  before mounting/fetching, and that function unconditionally sets the baseline.
  The existing `tile_recovery/stale_during_drag` checks the very first 60-pixel
  drag, a second drag, and anchored zoom while replacement tiles are held. It
  passed unchanged (race, 2.136s). Deliberately skipping baseline initialization
  failed all three geometry assertions with an unmoving retained image. The
  mutation was restored; the final individual race test passes (2.138s). No
  unnecessary viewport change is included.
- Progressive demand confirmed. Before the fix, a second individually delivered
  source increased HTTP requests from six to eight immediately. The single
  `tile_recovery/progressive_demand` test now uses `testing/synctest`, held reads
  and HTTP counts to prove immediate initial tiles, coalesced intermediate fits,
  immediate final fit, immediate manual movement, retired automatic adjustment
  after manual input, and terminal Stop without waiting for the timer. Final
  individual race PASS (3.311s). The separate
  `camera_retention/progressive_discoveries` race passes (2.089s).

Commands use `go test -race -tags no_emoji,nodynamic -count=1 -run '^NAME$'
with `./internal/ui/locationmap` for storage tests and anchored child paths under
`^TestLocationMap$` with `./internal/ui` for viewer cases. Red and deliberate
mutation runs omitted `-race`. No package-wide or full local suite was run.

GoLand all-warning inspection completed for all six changed Go files:
`locationmap/{favorites.go,favorites_test.go,feature.go,surface.go,work.go}` and
`internal/ui/locationmap_test.go`. Removed a redundant declared type and moved
fixture cleanup outside its loop; documented two false-positive partial-inventory
nil warnings with narrow existing-style suppressions. Reinspection is clear.
One cancelled IDE call was retried successfully; no incomplete scan is counted
as clean. Revision: working tree based on `7a161a2`; Go diff SHA-256
`0cf2bfd24e46156d73906e47d7125c982d9d2b7a504245fae73f30804263348a`.
Profile identity is not exposed; Qodana CI remains waived, not passed.
Updated architecture and Done changelog with the camera cadence and terminal
cache-cancellation behavior. No new files, dependencies or root-UI test names.
Formatting, exact Qodana exclusions and the 706-test shard inventory pass;
`git diff --check` is clear. Full suites/build/vet remain assigned to GitHub.

## Fresh review of 9b7ceaa

All twelve CI/CodeQL checks passed. Security completed at 11:58:55 UTC without
new actionable findings; code review completed at 11:59:56 UTC with two reports.
T0 retains review/fix ownership; no additional delegation.

- Confirm the P1 hidden-Grid state leak through the existing viewer seam: enter
  the map with a filename filter and an outside-cluster selection, open a cluster,
  assert complete membership and safe clipboard targets, then restore the original
  visit. Make exact-occurrence cohort opening clear temporary search/selection;
  do not change ordinary hidden-Grid preservation or restored cluster visits.
- Assess the retry admission P2 against the ordered parent/retry barriers:
  `Feature.Wait` joins `workers` before `retryWorkers`, and retry admission occurs
  inside a still-counted parent. Run the existing retry and cancellation tests
  individually. Reject an incorrect report without adding synchronization.
- Inspect all changed Go files, run only individual tests and formatting/manifest
  checks, update Done, commit/push, reply/resolve, and obtain another fresh clean
  code/security review and GitHub suite. Unrelated concurrent architecture
  documentation changes remain unstaged.

### Assessment and evidence

The cluster finding is confirmed. The new single viewer regression
`TestLocationMap/cluster_visit/isolates_saved_grid_state` failed before the fix:
the two-member cluster showed only index 0, retained outside selection index 2,
and the stubbed OS clipboard received the outside image path. `OpenOccurrences`
now clears inherited selection/search after its defensive Close, before installing
the cohort. It does not change ordinary Close or the caller-owned saved visit.
The regression verifies safe clipboard targets and restored original search and
selection through two cluster visits. Final race PASS (2.633s). An initial fixture
compile error was corrected before the behavioral red run.

The retry admission report is rejected: `requestTiles` registers `retryWorkers`
inside the outer `workers.Go` function, before that parent's deferred Done.
`Feature.Wait` and hidden `Settle` both wait for `workers` first and only then
wait for `retryWorkers`. Thus a parent paused between the context check and
retry admission prevents the first barrier from completing. Cancellation after
that check may create a promptly cancelled child, but it cannot escape the join
or race a zero-counter retry Wait. Retry UI delivery also rechecks context and
revision before requesting more tiles. No production synchronization was added.

Other individually run race regressions passed:

- `TestLocationMap/cluster_visit/frozen_progressive_members_and_escape_stages`
  (2.247s): ordinary cluster search/selection and staged Escape remain intact.
- `TestLocationMap/tile_recovery/retry_and_restore` (2.178s): admitted retry work
  is joined before restored tile delivery.
- `TestLocationMap/tile_recovery/hidden_direct_visit` (2.118s): held HTTP work
  cancels and settles after hiding/closing the map.
- `TestLocationMap/cluster_visit/navigation` (2.733s): cluster image navigation
  and image-to-Grid-to-map return remain intact.

All commands use `go test -race -tags no_emoji,nodynamic -count=1` with the
individual path anchored at each slash and `./internal/ui`; the red run omitted
`-race`. Full suites/build/vet remain assigned to GitHub.

GoLand all-warning inspections, including weak warnings, completed clear on both
changed Go files (`internal/ui/grid/subset.go`, `internal/ui/locationmap_test.go`).
Scope: working tree based on `9b7ceaa`, Go diff SHA-256
`0cf5d76e2d3f1647f82f508c5ed8866d83a586a517441a87bf268d2c2e67b1ab`.
Profile identity is not exposed; disabled Qodana remains waived, not passed.
No new files, dependencies, translation keys or top-level UI test names.
The Done changelog describes full cluster membership, safe targets and restored
original interaction state. Only this Done hunk is included from `todos.md`;
concurrent Open/backlog architecture edits are preserved outside the commit.
Formatting, exact Qodana exclusions and the 706-test shard inventory pass;
`git diff --check` is clear.

## Ledger

| Task | Spawns budget/actual | Reviews | Full local suite |
| --- | --- | --- | --- |
| Recon | 1/1 | T0 verified scout locators | No |
| Fixes and gate | 0/0 | T0, sixteen findings fixed and one rejected with regression evidence | No; GitHub CI; pause after final requested batch |
| Resumed a5b250d recon/fixes/gate | 1/1 scout; 0/0 fixes | T0; two product fixes, native timing containment, CI assertion and pan protocol correction | No; individual local tests, GitHub full suite |
| Renewed c02f6ed review | 1/1 scout; 0/0 fixes | T0; one input-ownership defect reproduced and fixed | No; individual local tests, GitHub full suite |
| 7a161a2 remote round | 0/0 | T0; two confirmed findings fixed, one false positive negatively verified | No; individual local tests, GitHub full suite |
| 9b7ceaa remote round | 0/0 | T0; cluster targeting reproduced and fixed, retry false positive rejected by ordered barriers | No; individual local tests, GitHub full suite |
