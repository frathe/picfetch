# PR #38 review and verification

Route: Standard. Lead owns the standards/spec review and every fix. Scope is
the response-body timeout and GitHub JSON bounds introduced by PR #38; no
dependency changes, merger, release, or independent verifier redesign.

## Findings and acceptance criteria

- A queued `time.AfterFunc` callback can expire a response after a successful
  read resets its timer. Recheck the current inactivity deadline under the same
  mutex before closing a response.
  Verify: `go test -tags no_emoji,nodynamic ./internal/ui/autoupdate -run TestIdleTimeoutReadCloser_StaleExpiration`.
- Replace the 25 ms progress test's scheduler-dependent sleeps with virtual time
  and retain an HTTP integration test for a stalled body and context cancellation.
  Verify: `go test -race -tags no_emoji,nodynamic ./internal/ui/autoupdate -run 'Test(UpdateHTTPClient|IdleTimeoutReadCloser)_'`.
- Preserve bounded GitHub release/attestation decoding and existing updater behavior.
  Verify: `go test -race -tags no_emoji,nodynamic ./internal/update ./internal/ui/autoupdate`.
- Inspect all four changed Go files with GoLand; run formatting/vet/build checks.
  Hosted CI owns the broad suite, per the explicitly invoked review-loop rules.
- Mark ready after fixes, push, and obtain fresh Codex code/security reviews,
  inspect post-suppression Qodana SARIF and CodeQL alerts, and wait for required CI.
  Final commit-bound evidence belongs in the PR conversation, avoiding another
  documentation-only push invalidating completed reviews.

## Tasks and delegation

1. Lead: reproduce the stale callback through the existing response-body seam,
   fix it in `updater.go`, and update `updater_test.go`. One regression at a time.
2. Lead: review GitHub JSON bounds/tests, inspect changed code, and maintain this
   evidence record and `todos.md`. No interface or package changes planned.
3. Lead: commit/push fixes, ready the PR, and own the hosted review loop.

One read-only scout inventories HTTP consumers and cancellation/closure ownership
while the lead reviews. G1: bounded call inventory; G2: references checked with
`rg`/targeted reads; G3: zero edits; G4/G5: independent dependency/caller context.
Script-before-spawn does not answer cross-package and dependency ownership by
itself. The scout performs no review or fixes. Budget: one scout, zero delegated
implementations, focused local tests and one hosted full suite per changed head.

## Evidence

- Initial head: `87a5ae7c4f77abf42ad563dbf1dcd0ce065d0f6d`; no review threads.
- Initial GoLand inspection of all four changed Go files reported no findings.
- Initial GitHub Advanced Security job failed before reviewing code because its
  service rejected the configured model (`400 The requested model is not supported`).
  Recheck on the final head; do not treat this as a clean security review.
- Standards: named the newly added reader's unused argument `_`, as required by
  `AGENTS.md`. No new package, dependency, test file, translation, or root UI test.
- Spec: confirmed the stale-expiration defect with the new deterministic test:
  original code failed with `update response body idle timeout: EOF`; the
  deadline recheck passed. Replaced the narrow wall-clock progress test with
  `testing/synctest` and retained real HTTP cancellation/timeout integration.
- Extended JSON tests to cover exact-limit acceptance and reading at most one
  byte beyond the limit. Existing release/attestation callers both use the bound.
- `go test -race -tags no_emoji,nodynamic ./internal/update ./internal/ui/autoupdate`
  passed (`internal/update` 3.699s; `internal/ui/autoupdate` 2.189s).
- `make fmt` and `make verify-build` passed, including vet/build, TUF expiry,
  generated assets, updater dependency notices, and Qodana test exclusions.
- GoLand reinspection of all four PR Go files, including warnings, completed
  with no findings. Initial post-suppression `/qodana.sarif.json` has zero results;
  `/start/` is a baseline snapshot, not the final differential report.
- Actual delegation: one read-only scout; review and all edits remained inline.
  Local broad race suite intentionally left to hosted CI, as the loop requires.

## Hosted handoff

Local review findings are fixed. Push the fix commit, mark PR #38 ready, and
track the fresh review loop in its conversation. The PR must stay open for human
acceptance; retain this active record until acceptance.

The branch was marked ready after pushing `97331a9`. Both Codex reviews started,
but CI was withheld because the new review entry in `todos.md` conflicted with
the JPEG metadata entry on main (`98b560b`). Merge main into this branch while
preserving both entries; no updater code conflict or behavior change. The fresh
merged head must receive its own reviews and CI. GitHub's separate AI code
scanning service repeated its unsupported-model failure on `97331a9`; Codex's
own security review is a distinct service.
