# PR #39 review and verification

Route: Standard. Review base: `98b560bcc9c31b8043ffb3a3997dfaf0241b83b1`;
initial head: `13576b7bad2413f4e3b1f9e1a921075188f22d91`.
During review the user rebased the PR onto `2f021dc36ea7391e989434b1d020ec61eac3f2db`;
the local checkout was synchronized to `bd3ca90e592c212d0b0355d7412485ac30b6fc1f`
before committing review changes. The Favorites patch is unchanged by that rebase.
The lead owns standards/spec assessment and all fixes. Scope is private Favorite
storage and its startup error boundary; no dependency changes, merge or release.

## Assessment and acceptance criteria

- Standards: the implementation follows the documented storage and startup
  boundaries. Initial GoLand inspections of all three changed Go files were
  clear. The plan's startup command selected no tests, so it did not establish
  its claimed behavior. Replace it with a real early-failure regression.
  Verify: `go test -race -tags no_emoji,nodynamic ./internal/ui -run '^TestRun_RejectsUnavailableFavoriteStorageBeforeBuildingViewer$'`.
- Spec: no production defect confirmed against the stated scope. Strengthen
  the privacy regression to exercise a new Favorite and replacement of an
  actual legacy `0644` list in a `0755` directory. Ensure fallback requests use
  separate private children and filesystem creation errors retain their cause.
  Verify: `go test -race -tags no_emoji,nodynamic ./internal/favstore`.
- Verify nearby runtime/menu and preview wiring through focused root UI tests.
  Keep the new root UI test assigned in the shard manifest.
  Verify: `make check-test-shards` and the focused UI command recorded below.
- Inspect all changed Go files, including weak warnings; run `make fmt` and
  `make verify-build`. Hosted CI owns the complete race/platform suites.
- After pushing, mark ready and obtain fresh Codex code/security reviews for
  the latest head. Inspect post-suppression Qodana SARIF and CodeQL findings,
  resolve confirmed/rejected threads with evidence, and repeat reviews as needed.

## Tasks and delegation

1. Lead: strengthen existing storage tests and add the startup failure test to
   `preferences_wiring_test.go`; assign it in `.github/testshards/internal-ui.tsv`.
   Negatively verify the new guards, then restore production code.
2. Lead: maintain this record, original acceptance commands and `todos.md`;
   run focused tests and local build/inspection checks.
3. Lead: commit/push, mark ready, and own the hosted review loop. Final
   commit-bound results belong in the PR conversation to avoid a documentation
   push invalidating completed checks.

One read-only scout inventoried Favorite storage consumers while the lead
reviewed. G1: bounded caller/storage inventory; G2: references checked by `rg`
and targeted reads; G3: no edits; G4/G5: independent cross-package caller context.
Script-before-spawn alone does not establish those caller/worker relationships.
Budget: one scout, zero delegated implementations/reviews/fixes, focused local
tests and hosted full suites for each pushed head.

## Limits

Legacy Favorites become private when re-saved; startup migration is explicitly
outside the original spec. The random temporary fallback is session-specific:
later launches do not rediscover it. Windows ACL confidentiality is not established
by POSIX mode assertions. The startup environment regression exercises Linux;
hosted platform suites provide the other existing platform coverage.

## Evidence

- Initial review threads: none. Initial three-file GoLand inspection: no findings.
- `go test -race -tags no_emoji,nodynamic ./internal/favstore
  ./internal/ui/favorites ./internal/favthumbs` passed (1.110s, 3.699s, 1.306s).
- Focused root UI race regressions passed (2.250s):
  `TestRun_RejectsUnavailableFavoriteStorageBeforeBuildingViewer`,
  `TestStartViewerRuntime_ReplacesConstructionStopAfterGeometryRestoration`,
  `TestSyncFavoritePreviews_OpeningAFavoriteWritesPreviews`.
- Negative verification: temporarily restoring `0755`/`0644` publication failed
  both new/legacy privacy cases; swallowing fallback creation errors failed the
  error-cause guard; swallowing startup's error failed its guard with a nil error.
  Both production files were restored byte-for-byte, then tests passed.
- `make fmt` and `make verify-build` passed, including vet/build, TUF root,
  generated assets/notices and Qodana test exclusions. No new test files.
- GoLand reinspected all four Go files in the PR, including warnings, with no
  findings and no timeouts.
- Initial hosted CI passed all jobs. Initial post-suppression Qodana
  `/qodana.sarif.json` has zero results; CodeQL has no open alerts for the PR.
  These initial-head results do not substitute for checks on the pushed fixes.
- Actual delegation: one read-only inventory scout; all review and edits inline.
- `make check-test-shards` passed in Linux/amd64 Docker: 688 root UI tests,
  exactly assigned across three shards.
- Rebase synchronization preserved the upstream TODO reorganization and restored
  only the PR #39 entry to its existing Open section below Done. Focused tests
  and `make verify-build` passed again on the updated base; root UI took 2.265s.
  `git range-diff` confirmed the original Favorites patch is identical.
