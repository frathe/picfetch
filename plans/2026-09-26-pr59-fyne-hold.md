# PR 59: retain Fyne v2.8.0

## Scope and acceptance

Ronin requested removal of PR #59's Fyne v2.8.1 upgrade because of an
upstream regression. Restore Fyne v2.8.0 and its prior dependency closure;
retain the independent Sigstore v1.11.0 update. No application behavior,
dependency automation policy, merge or release is in scope.

- `go list -m fyne.io/fyne/v2 fyne.io/systray github.com/fredbi/uri github.com/sigstore/sigstore`
  must select v2.8.0, v1.12.2, v1.1.1 and v1.11.0 respectively.
- `go mod tidy -diff` must be empty; `go mod verify` must pass.
- `make verify` is the final repository gate. Record unavailable checks honestly.
- The final PR diff must contain no Fyne upgrade and the PR description must
  explain the hold while describing the remaining Sigstore update.

## Work and ownership

Standard route: dependency files, existing deferred-work record and notices.
The lead owns edits, dependency decisions, review and final verification.
One read-only scout checks Go/Docker availability while the lead edits; this
bounded environment query needs no shared design or writes, and its commands
provide the evidence. No implementation or review is delegated.

1. Restore the Fyne requirements and regenerate module checksums (`go.mod`,
   `go.sum`); verify module selection and tidy consistency.
2. Verify Sigstore license/source continuity and update its existing manifest
   and generated notice version (`scripts/updaternotices/manifest.json`,
   `THIRD-PARTY-NOTICES.md`); run the notice generator/checker.
3. Update `todos.md`, verify, commit and push to PR #59's existing branch, then
   update its title/body. Preserve the original workspace branch.

## Dependency provenance

Fyne v2.8.0 and systray v1.12.2 restore the already reviewed baseline and
existing shipped notices. Sigstore v1.11.0 is retained from PR #59, sourced
from https://proxy.golang.org/github.com/sigstore/sigstore/@v/v1.11.0.zip.
Its Apache-2.0 license and copyright obligations must remain delivered in
`THIRD-PARTY-NOTICES.md`, embedded in the app and included in release artifacts.
`diff -qr` of the complete cached v1.10.10 and v1.11.0 module trees found only
the addition of `RELEASING.md`; all shipped source, nested files and license
texts are unchanged. SHA-256 of `COPYRIGHT.txt` and `LICENSE` matches the
existing reviewed manifest (`6622d1d4...` and `cfc7749b...` respectively).
The six-target updater closure check passed as part of `make verify`.

## Verification evidence

Verified on PR head `13666d9ef9b4654927a21295d492212627362e51` plus this change,
using Go 1.27.1 from `/snap/go/current/bin` on native Linux/amd64:

- Module selection: Fyne v2.8.0, systray v1.12.2, URI v1.1.1, Sigstore v1.11.0.
- `go mod tidy -diff`: empty; `go mod verify`: all modules verified.
- `make generate-updater-notices`: success; generated diff changes only the
  Sigstore version and source URL.
- `make verify`: exit 0, including formatting, TUF root, Qodana exclusions,
  generated assets, updater/AVIF notices, vet, build, shard inventory and all
  four native Linux/amd64 Docker race partitions. UI partitions passed in
  335.351, 343.034 and 358.095 seconds.
- Race evidence retained locally in
  `/tmp/picfetch-pr59.EqoOCJ/.scratch/race-runs/20260926T142018Z-bsHRUy`.
- `git diff origin/main -- go.mod go.sum`: only the Sigstore version and its
  checksums remain changed. `git diff --check`: clean.

The initial dependency rollback changed no Go source or tests. Its pushed commit
was `6e60afb86ade84709ba9e8922ff2dd4cce8c26d3`. The follow-up below addresses the
Validation failure on that commit; no merge or release is part of this task.

## CI follow-up: AVIF notices with an empty module cache

Ronin requested fixing remaining CI failures while ignoring the external FOSSA
License Compliance result. That specific check is waived for this task, not
passed or disabled. All other checks on `6e60afb` passed except Validation job
`108422979818`, which failed `make check-avif-notices` with `LICENSE changed`.

The same command failed with an isolated empty `GOMODCACHE` and passed after
downloading only the two existing pinned modules. `go list -m -json` returned
the correct versions but omitted `Dir`; the checker then hashed the checkout's
`LICENSE`. No dependency, payload, hash, retained license or shipped notice
changes are needed.

The lead fixes `scripts/avifnotices/main.go` to materialize the two selected
module archives before listing and to reject missing directories. Regression
coverage goes in the existing `main_test.go`: an offline file-based Go proxy
exercises the real command with an empty cache, then checks offline warm reuse;
the existing rejection matrix covers an absent directory with a matching file
in the working directory. README and this evidence record describe the behavior.

One bounded read-only scout searched the two existing notice tools for reusable
module-resolution/test helpers; none already modeled a Go proxy. No edits or
review were delegated. Lead verification: focused race tests for both notice
tools, the original cold-cache command, GoLand inspections of both changed Go
files, build validation and fresh GitHub checks. The prior local full race gate
and all GitHub race/native jobs already passed; CI runs the full suite again.

Red evidence: both new cases failed before the fix (0.012 seconds), reporting
the exact `LICENSE changed` error and acceptance of an absent module directory.
Green evidence:

- `go test -tags no_emoji,nodynamic -race ./scripts/avifnotices
  ./scripts/updaternotices -count=1`: both packages pass, including the real
  Go subprocess against an offline file proxy and missing-directory rejection.
- The original `make check-avif-notices` succeeds in a second empty cache
  (`/tmp/picfetch-pr59-avif-fixed.LULxLb`) and succeeds again with `GOPROXY=off`.
- `make verify-build`: exit 0; formatting, notices, generated assets, vet and
  build passed. No new test file or root-UI test requires an exclusion/shard edit.
- `git diff --check`: clean. Module requirements, checksums, license manifest
  hashes and shipped notice texts are unchanged by this follow-up.

GoLand fallback inspections were attempted on both changed Go files through
the existing IDE session. The connector rejected `main.go` as outside the open
project and rejected the worktree project path for `main_test.go` because that
project is not open. Inspection findings, including weak warnings, remain
unverified; command-line vet and race tests are separate evidence. The local
inspection guide was read from `origin/feature/image-map` because this older
PR base does not contain it. The disabled Qodana CI gate remains waived.

Fresh GitHub checks on the follow-up commit are pending; License Compliance
alone remains excluded from this task's completion criteria.

## Cost ledger

| Task | Spawns (budget/actual) | Review rounds | Full suite |
|---|---|---|---|
| Environment discovery | 1 / 1 | Lead assesses command output | No |
| Edit and verify | 0 / 0 | 1 | Once, passed |
| CI follow-up | 1 / 1 read-only scout | 1 | Fresh CI; focused local race tests |
