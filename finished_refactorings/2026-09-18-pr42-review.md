# PR #42 review and verification

Route: Standard. Base: `bf01706c9468245abac9b149a7ad7180b60ff3ad`;
initial head: `d9deb3af5b7f28cc21133e705137081ce4358876`.
Spec: PR #42 pins the four external actions in the protected Microsoft Store
publisher job to reviewed full commits, preserving its approval and command flow.
The lead owns standards/spec review, fixes and the hosted review loop.
No Store operation, merge or release is authorized by this work.

## Assessment and acceptance criteria

- Spec: all four references match their stated upstream release tags; no
  execution, permission, artifact or approval behavior changed.
- Standards: the existing workflow contract did not enforce immutable action
  references. Extend that contract and prove it rejects a mutable reference.
- GoLand's undefined-input warnings are false positives: the exact upstream
  action manifests declare every referenced input. Scope `UndefinedParamsPresent`
  suppressions to the affected input mappings (one individual preparation input).
- AC1: protected action references remain full commit SHAs.
  Verify: `go test -tags no_emoji,nodynamic ./scripts/msixstage -run '^TestStoreWorkflowPublishingContract$' -count=1`,
  including a temporary mutable-tag negative check followed by restoration.
- AC2: Store command and packaging contracts remain green.
  Verify: `go test -tags no_emoji,nodynamic ./scripts/storepublish ./scripts/msixstage -count=1`.
- AC3: `actionlint` v1.7.12, YAML parsing, `git diff --check`, and GoLand
  inspections of both changed code files complete without actionable findings.
- AC4: push fixes, mark ready, and obtain fresh clean Codex code/security reviews,
  inspect post-suppression Qodana SARIF and CodeQL findings, and pass required CI
  on the latest head. Final commit-bound receipts belong in the PR conversation.

## Tasks and delegation

1. Lead: add the contract assertion in existing `scripts/msixstage/msixstage_test.go`,
   negatively verify it, and add narrow workflow inspection suppressions.
2. Lead: focused tests, workflow validation, GoLand inspections, and this record
   plus `todos.md`. No new test file, UI test, package, or dependency.
3. Lead: commit/push, readiness and the hosted review loop. Full suites run in CI.

One read-only scout collected upstream pin/runtime/input/license facts, then
the official inspection ID and YAML suppression syntax. G1: bounded source
questions; G2: verify API values and inspector output; G3: no edits; G4/G5:
independent upstream metadata while the lead reviewed local contracts.
Budget: one scout plus one follow-up; no delegated review or fixes; focused
local checks and full hosted CI per pushed head.

## Dependency provenance and distribution

All references are official GitHub actions, MIT licensed, with Node 24 entry
points and no nested action references in their manifests:

| Action | Release | Commit |
| --- | --- | --- |
| actions/checkout | v7.0.1 | `3d3c42e5aac5ba805825da76410c181273ba90b1` |
| actions/download-artifact | v8.0.1 | `3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c` |
| actions/setup-go | v7.0.0 | `b7ad1dad31e06c5925ef5d2fc7ad053ef454303e` |
| actions/upload-artifact | v7.0.1 | `043fb46d1a93c77aae656e7c1c64a875d1fc6a0a` |

Sources: each `https://github.com/actions/<name>/blob/<commit>/action.yml`
and `LICENSE`, and upstream tag/release API records. Actions execute unmodified
in GitHub CI; their upstream distributions retain the license notices. No action
code or runtime is vendored into PicFetch or its distributed app artifacts, and
the app's shipped dependency closure and notice delivery remain unchanged.

## Evidence

- Initial unresolved threads and bot reviews: none; PR was draft.
- Initial focused Store publisher and packaging tests passed.
- Initial actionlint v1.7.12 and Ruby YAML syntax validation passed.
- The new guard rejected a temporary `actions/checkout@v7` reference with the
  intended immutable-commit diagnostic; the pinned workflow was restored.
- Both focused packages passed again after the guard and suppressions:
  `scripts/storepublish` (1.109s) and `scripts/msixstage` (0.590s).
- Final actionlint v1.7.12 (shellcheck disabled), Ruby YAML parse, gofmt and
  `git diff --check` passed. Existing shell scripts were unchanged.
- GoLand inspected both changed code files with warnings enabled after the
  scoped suppressions: no findings or timeouts. Its build tool reported success
  but cannot collect build diagnostics for this project; command tests provide
  the compilation evidence.
- Initial Qodana run `35321989522` had zero results in authoritative
  `/qodana.sarif.json`. Latest-head results must be inspected separately.
- Local implementation accepted. Final commit-bound hosted reviews, dispositions,
  Qodana/CodeQL results and CI evidence are maintained on
  [PR #42](https://github.com/frathe/picfetch/pull/42) to avoid another unreviewed
  documentation commit after the final checks complete.
- Actual delegation: one read-only scout and one bounded follow-up; all review
  and fixes by the lead. Focused local checks, complete suites in GitHub CI.
