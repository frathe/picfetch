# PR #35 review and verification

Route: Standard. Lead owns review and all fixes. Scope is Save Changes' selected
leaf semantics and the existing Save/Strip/Export transaction contract. No new
dependencies, UI strings, packages, or release/merge actions.

## Acceptance criteria

1. A queued metadata removal follows the selected file after a preceding save
   replaces its symlink. The original target remains unchanged.
   `go test -tags no_emoji,nodynamic ./internal/imaging -run TestFileMutationsSerializeWholeTransactionsAcrossAliases -count=1`
2. Save/Strip/Export still serialize aliases, preserve cancellation and committed
   results, and reject Export symlink leaves.
   `go test -tags no_emoji,nodynamic ./internal/imaging -count=1`
3. Save and file-write reconciliation remain correct in the viewer.
   `go test -tags no_emoji,nodynamic ./internal/ui -run 'Test(CanSaveRotation|SaveRotation|SaveChanges|Export)' -count=1`
4. Formatting and GoLand inspections cover all four changed Go files. Latest-head
   CI, Codex code/security review and post-suppression Qodana/CodeQL have no
   actionable findings before the review loop finishes.

## Tasks

- Lead: add the queued Save/Strip regression to `mutations_test.go`, observe red,
  revalidate resolved paths after transaction admission in `mutations.go`, and
  rerun criteria 1 and 2. Fix stale save-policy comments in `save.go`/`save_test.go`.
- Lead: run criterion 3 and formatting/IDE checks, update `todos.md` and PR text,
  commit/push fixes, mark ready, then request and assess fresh hosted reviews.
- Evidence scout: collect GitHub threads/checks and analyzer artifacts into
  `/tmp/picfetch-pr35-evidence`; no code review, edits, or GitHub writes.
  Delegation gate: self-contained API inventory; saved JSON/SARIF is the oracle;
  no repository writes; independent of local code context; no hot review context
  delegated. API/artifact traversal benefits from a bounded scout alongside review.

Budget: one evidence scout, lead fixes inline; focused local tests only, full suite
on native CI as required by the invoked GitHub review workflow. Repeat hosted
reviews after every finding disposition. No new test file or UI top-level test.

## Evidence

- Initial head `762db5a7`: CI green; no Codex review or inline threads yet.
- Initial GoLand inspections: all four changed Go files clean, including warnings.
- Local Go uses `/snap/go/current/bin` because the `/snap/bin/go` launcher cannot
  initialize its sandbox inside the execution sandbox.
- Confirmed P2: queued Strip retained the original symlink target after a Save
  replaced the selected leaf. The regression failed with both the saved-file
  metadata retained and the original target changed. Resolving again after
  admission, releasing/retrying on a changed destination, fixes both assertions.
- Full imaging package: passed (`3.859s`). Focused viewer Save/Export regressions:
  passed (`5.459s`). `make fmt-check vet` and `make build`: passed. Repeat GoLand inspections of
  all four changed Go files: no errors or warnings, no incomplete results.
- Original-head Qodana post-suppression SARIF and CodeQL: zero findings. No
  existing threads required disposition.
- Implementation accepted locally. Fresh hosted review dispositions and the final
  commit-specific CI/security/Qodana/CodeQL evidence are recorded on
  [PR #35](https://github.com/frathe/picfetch/pull/35); an older clean run does not
  satisfy that gate. The PR remains unmerged by this workflow.

## Cost ledger

One read-only evidence scout (budget/actual 1/1); review and fixes stayed with
the lead. One local red/green round; no full local race suite (hosted CI owns it).

## Limits

This change coordinates PicFetch transactions and preserves selected leaf writes.
It does not add filesystem-wide exclusion against other processes or pin mutable
parent directories. Hosted platform results are required for cross-platform claims.
