# PR #43 review and verification

Route: Standard. Base: `1cfaa5205bfb737b197dcd8f8be4f2b4b4f4ad89`;
initial head: `2da1a1b2ab110ec1812fdad45801700ad5be1d3f`.
Spec: PR #43 bounds Copy Selection PNG encoding without another full raster.
The user requires its byte limit to follow the existing Max file size (MB)
setting, followed by Ready for review and the GitHub Codex review loop.
No merge or release is authorized. Lead owns all assessment and fixes.

## Assessment and acceptance criteria

- Spec: replace the hard-coded 64 MiB limit with one captured
  `imaging.MaxEncodedBytes()` value per PNG encoding. This is the existing
  process-wide policy set by `viewer.SetMaxFileSizeMB`.
- Spec: bound the PNG encoder's scanline storage as well as the selection's
  pixel footprint and encoded output; width alone can dominate encoder memory.
  Limits describe these bounded components, not an exact total heap ceiling:
  compression state and buffer growth add overhead.
- Spec: retain the former crop's 8-bit NRGBA conversion; forwarding arbitrary
  source models can silently select 16-bit PNG output and double row storage.
- Standards: retain recoverable clipboard errors and the existing allocation-free
  crop view; no new globals, interfaces, dependencies or user-facing strings.
- AC1: small synthetic inputs prove configurable pixel admission, row admission,
  exact encoded-output bounds, one captured limit and eight-bit color output.
  Verify: `go test -tags no_emoji,nodynamic ./internal/ui/copyselection -count=1`.
- AC2: changing the viewer's Max file size setting rejects a selection without
  clipboard dispatch, keeps it editable, and permits a retry after raising it.
  Verify: `go test -tags no_emoji,nodynamic ./internal/ui -run '^TestCopySelection' -count=1`.
- AC3: formatting, shard/exclusion checks, vet/build and GoLand inspections of
  every changed code file have no actionable findings.
- AC4: latest pushed head has fresh clean Codex code/security reviews, inspected
  post-suppression Qodana/CodeQL results and passing required CI. Final receipts
  belong in the PR conversation to avoid an unreviewed documentation-only head.

## Tasks and delegation

1. Lead: add regression tests to existing PNG and root worker test files, observe
   their bounded failures, then fix `internal/ui/copyselection/png.go`.
2. Lead: update the UI shard manifest, run focused regressions and inspections,
   and record results here and in `todos.md`. No package moves or new test files.
3. Lead: commit/push, mark ready and finish the hosted review loop. Complete
   suites run in CI; do not duplicate the broad local race suite.

One read-only scout gathers hosted check/report evidence while the lead reviews
the encoder. G1: bounded report retrieval; G2: verify SHA and SARIF/API output;
G3: no source edits; G4/G5: independent CI metadata, no duplicated local review.
Budget: one scout with follow-ups; focused local gates and full hosted CI per head.
No dependencies change, including the shipped runtime/model closure or notices.

## Evidence

- Initial unresolved threads and Codex reviews: none; PR was draft.
- Initial Qodana run `35326633397`: exact top-level post-suppression SARIF has
  zero results and matches the initial head. Latest-head evidence is still needed.
- PNG tests first failed for ignored configured pixel/output limits, missing
  scanline admission and changed 16-bit output. All fixtures use small budgets
  and images; no exhaustion attempt is needed.
- The UI regression first failed because an over-budget selection completed
  successfully. After the fix it stays active, makes no clipboard call and
  succeeds after changing Max file size from 1 MB to 2 MB.
- Focused PNG/Source/Feature and all Copy Selection UI regressions passed:
  `internal/ui/copyselection` (0.038s), `internal/ui` (2.882s). The complete
  `internal/ui/copyselection` package also passed (0.160s).
- GoLand inspected all three changed Go files with warnings enabled: no
  remaining findings or timeouts. Two existing duplicated test-fixture fragments
  received function-scoped suppressions, consistent with the repository's
  Qodana test exclusion policy; expectations and drag semantics stay explicit.
- Initial-head GitHub CI, Qodana and both CodeQL languages completed green;
  these do not satisfy the required final-head review gate.
- `make fmt-check`, `make check-qodana-test-exclusions`, `make check-tuf-root`,
  `make vet` and `make build` passed. Public Docker `make check-test-shards`
  passed with all 689 UI tests assigned across three shards.
- A temporary Go build overlay made the output writer reread live policy;
  `TestPNG_CapturesSizeLimitBeforeReadingPixels` rejected that change with its
  intended diagnostic. Unmodified-source verification passed again (0.010s).
- Local implementation accepted; final commit-bound hosted reviews, dispositions,
  Qodana/CodeQL results and CI receipts are maintained on
  [PR #43](https://github.com/frathe/picfetch/pull/43).
- Actual delegation: one read-only scout; all review and fixes by the lead.
  Focused local tests and native build; complete race/platform suites in CI.
