# PR #44 review and verification

Route: Standard. Base: `053b9d0`; initial head: `ee61d8a`.
Spec: PR #44's description requires bounded, cancellable single-file sibling
listing while preserving opened-file identity, filtering and deduplication.
The user requests an explanation of the 256-entry batch, fixes for confirmed
findings, Ready for review and the complete GitHub Codex review loop.
Lead owns all review and fixes; no merge or release is authorized.

## Acceptance criteria and tasks

1. Lead: assess the PR against its spec and repository standards, then add
   focused regression coverage before fixing confirmed defects in filescan.
   Verify: `go test -tags no_emoji,nodynamic ./internal/filescan -count=1`.
2. Lead: verify single-file opening, formatting, vet/build, exclusions and
   GoLand inspections for every changed Go file. Focused UI command:
   `go test -tags no_emoji,nodynamic ./internal/ui -run '^TestHandleDrop_SingleFile' -count=1`.
3. Lead: update this evidence and `todos.md`, commit/push, mark ready, and
   complete fresh Codex code/security review plus required CI on the latest
   pushed commit. Inspect post-suppression Qodana and CodeQL results.
   Keep final hosted receipts in the PR to avoid an unreviewed documentation head.

One read-only scout retrieves hosted reports while the lead assesses local code.
G1: bounded CI/report retrieval; G2: verify run/head SHA and SARIF/API output;
G3: no source edits; G4/G5: independent hosted evidence, no duplicated review.
Budget: one scout with follow-ups, focused local verification, full suite in CI
per the authorized GitHub loop. No dependencies or package boundaries change.

## Evidence

- Initial PR is draft with no reviews or unresolved threads.
- `batchSize = 256` bounds entries per directory read, not the total scan.
  The captured `maxScan` controls total entries and image admission; the entry
  budget also counts unsupported files and directories, plus one EOF probe.
- Spec finding: `File.ReadDir` returns filesystem order whereas Fyne's former
  `storage.List` returned name order. Sort the bounded admitted siblings while
  retaining the opened URI first; do not enumerate more entries to choose a
  globally alphabetic subset of an over-budget directory.
- Spec finding: entry-budget truncation reused a toast claiming `maxScan`
  images were gathered, even when only the opened image was found. Replace it
  with a scan-limit message in English and German, without claiming an image
  count. No new top-level UI tests or test files; existing shard/exclusions apply.
- Standards assessment: no actionable design/concurrency violations. GoLand's
  two existing duplicate-fixture warnings were mitigated with function-scoped
  suppressions, retaining independent traversal and complete UI-path assertions.
- Red: the 300-image regression failed on unordered siblings; the truncation
  UI regression failed on its old message, including a one-image cluttered
  directory. Both passed after the fixes.
- Filescan tests pass (0.040s), including 300 images across batches, cancellation
  after image 260, and entry limits below/at/above a 21-entry directory boundary.
  Focused single-file/truncation UI tests pass (0.273s); translation tests pass
  (0.020s).
- Initial-head CI run `35330850305` passed all eight jobs. Qodana run
  `35330850154` post-suppression SARIF has zero results and matches `ee61d8a`.
  CodeQL run `35330850260` passed; Go and Actions report zero results on merge
  `7faeba2`, whose second parent is the initial PR head. Latest-head receipts
  are still required after pushing the fixes.
- GoLand inspected all four changed Go files including weak warnings; all are
  clear after the two scoped test-fixture mitigations, with no timeouts.
- `make fmt-check check-qodana-test-exclusions check-tuf-root vet build` passed.
  `make check-test-shards` passed in Docker: all 689 UI tests assigned.
- Temporary build overlays separately broke EOF detection, restricted the scan
  to one batch, and removed per-image cancellation. The new tests failed for
  each intended reason; no mutation was made to the working source.
- Local implementation accepted. Final commit-bound hosted review/CI receipts
  and any later dispositions are maintained on
  [PR #44](https://github.com/frathe/picfetch/pull/44).
- Actual delegation: one read-only hosted-evidence scout with follow-ups;
  all code assessment and fixes by the lead. Full race/platform suites run in CI.
