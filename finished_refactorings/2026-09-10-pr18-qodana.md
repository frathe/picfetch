# PR #18 Qodana findings

Status: complete (2026-09-10). Route: Standard. The 18 findings on `d13c353` are the spec;
the breadth is mechanical cleanup across existing packages, with no new design.
Ronin authorizes inspection/file exclusions for tests and confirmed non-issues.
Lead owns all fixes and review. No commit, push or unrelated cleanup.

Scope amendment: Ronin also requested CodeQL alert #5 (`go/zipslip`) on PR #18.
The runtime extractor already checks exact allowlisted names and rejects links,
but joins the archive-derived string into filesystem paths. Select destination
names from fixed trusted entries instead. This preserves the acceptance policy
and removes the untrusted data flow; do not suppress the security query.
Verify the existing rejected-download tests plus `make explorer-install-test`
for the actual pinned runtime archive, and record that remote CodeQL needs a
scan of the pushed changes. No local CodeQL CLI is installed.

## Acceptance and tasks

1. Capture the exact CI SARIF report and account for every finding. Trace the
   optional cache's error/result behavior before excluding its warnings.
   Owner: lead plus one read-only cache scout. Oracle: SARIF locations and
   existing cache-failure tests traced to production functions, checked by lead.
2. Apply mechanical production cleanups and narrowly scoped exclusions.
   Owner: lead. Files: reported source files, `qodana.yaml`, `todos.md`.
   Preserve numerical rounding, chroma-subsampling fallback and best-effort
   caching. Fix an actual behavior defect through a failing regression first;
   add no tests that merely repeat mechanical changes or bitmap expectations.
   Verify: focused affected-package tests; report-to-fix/exclusion inventory;
   `make check-qodana-test-exclusions`; available GoLand/Qodana inspections.
3. Final gate and closeout. Owner: lead, depends on 1 and 2.
   Verify: `git diff --check`, `make verify`. Record actual Qodana rerun scope;
   never describe the unchanged remote PR report as cleared before a new scan.

## Delegation and budget

One read-only scout traces cache constructor/error/read/write behavior and tests
while lead inspects the other findings. G1: bounded ownership question; G2:
source locations plus existing executable tests; G3: no writes; G4: isolated
semantic call-chain tracing; G5: lead has not inspected that contract this turn.
S/W: comprehension is required, with no implementation delegated. Budget: one
scout, one lead review after focused checks, one full final gate. Mechanical
style fixes use existing behavior tests; genuine defects use TDD.

## Finding disposition

- Fix 5: two exported-element comments, the slice upper bound, the `copy`
  variable and the cohort validation error's capitalization.
- Exclude 13 by exact inspection/file: 3 fixture conversions, 6 intentional
  float64 rounding barriers, 1 incomplete-switch warning on the generic JPEG
  fallback, and 3 advisory cache-result warnings. No global inspection disables.
- Cache constructor inspection confirms an initialized map on every return;
  retained valid roots are closed even after partial initialization. Existing
  real-worker cache tests cover reuse, invalidation and corruption; constructor
  failure has no dedicated regression. No cache behavior was changed.
- CodeQL: archive names participate only in comparison against fixed names;
  filesystem paths and returned installation names derive from trusted entries.
  Duplicate-member, regular-file-only, size, checksum and completeness checks
  are unchanged. This is a data-flow clarification of an existing allowlist,
  not a reproduced traversal exploit. The original CodeQL alert and pre-fix
  Qodana/GoLand results supply the inspection failure evidence; no artificial
  behavioral tests were added for these semantics-preserving changes.

## Verification record

- Current-head SARIF inventory: all 18 accounted for (5 fixes, 13 exclusions).
  `python3 .scratch/qodana-pr18-20260910/check_findings.py` passes.
- GoLand inspections confirm removal of the five corrected findings and report
  no warnings in the changed installer. IDE-only notices are not Qodana's CI
  profile; YAML exclusions apply to Qodana, not the IDE profile.
- `go test ./internal/imaging -run '^TestScaleForExport' -count=1`: PASS
  (1.363 s). `go test ./internal/favstore -run '^TestCohorts$' -count=1`:
  PASS (0.311 s). Explorer minimum-zoom regression: PASS (1.075 s).
- `go test -tags explorertrial ./internal/ui -run
  '^TestVisualSimilarityExplorer(Local)?$/(favorite_cache|return_to_map|shift_pan)$'
  -count=1 -v`: PASS (4.323 s), including actual offline cache/inference behavior.
- `go test ./scripts/explorereval -run '^TestAssetInstall$' -count=1`: PASS
  (0.587 s). `make explorer-install-test`: PASS (80.993 s), with the actual pinned
  download, extraction, verified reuse and offline synthetic-image analysis.
- `make verify`: PASS, exit 0. Formatting, TUF, exclusions, vet, build, shard
  inventory and all four Linux race partitions pass. Artifact directory:
  `.scratch/race-runs/20260910T195810Z-VaU8yl`.
- Evidence and scanner limitations:
  `.scratch/qodana-pr18-20260910/README.md`.

Actual cost: one read-only scout, one lead review per cleanup/security slice,
one full gate. Additional installer qualification follows the user-requested
CodeQL scope amendment; no delegated implementation or review.
