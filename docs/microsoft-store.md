# Microsoft Store release

PicFetch uses the MSIX submission reserved in Partner Center under Store ID
`9P0DM0KTH01K`. Store packages are separate from the signed ZIP downloads:
Microsoft signs the MSIX bundle after certification, installs it under the
protected package location, and owns its updates.

## Build the bundle

The `Microsoft Store package` workflow runs automatically for every `v*` tag
and can also be started manually from GitHub Actions. It runs the full CI gate,
cross-builds x64 and ARM64 binaries with the `microsoftstore` build tag, stages
both packages, validates them with MakeAppx, creates and test-signs one bundle,
and runs the Windows App Certification Kit.

After WACK succeeds on a stable tag, the producer records the tag/commit,
workflow run/attempt, SHA-256 hashes and source release notes. Its 90-day artifact
contains `picfetch-microsoft-store.msixbundle`, `wack-report.xml` and
`store-release.json`. Manual/branch/prerelease builds do not produce an eligible
release record. The publisher downloads this exact artifact, verifies its
provenance and inner package manifests, and uploads the unchanged bundle inside
the ZIP required by Microsoft's API. It never rebuilds the application.

The bundle's temporary CI signature is trusted only on the runner; Partner Center
replaces it with a Microsoft signature after certification. Do not distribute
this bundle as an ordinary GitHub download.

## Approve each rollout

The separate `Microsoft Store publisher` workflow runs after successful Store tag
builds and through manual operational commands. The build, validation and note
preparation remain automatic. **Every rollout requires frathe's approval through
GitHub Required reviewers** on the existing `microsoft-store` environment.

The preparation job uses GitHub data only. It checks the saved environment policy,
validates the triggering producer run (or selects a requested/newest stable release
for manual submission), and freezes an `approval.json` artifact. The summary shows
the tag, commit, producing run/attempt, artifact ID, bundle/WACK hashes, expected
published base and exact generated notes. Recovery summaries also identify the
recorded submission and phase. Review that summary before approving the protected
job. No Microsoft credentials are available during preparation.

The protected job downloads that exact approval artifact by immutable ID and
checks its SHA-256 from the preparation job. Both jobs use the workflow run's fixed
`main` commit. After approval, the publisher restores the original validated build
by run/attempt/artifact ID, verifies tags, versions and hashes again, and compares
the live Store version and current receipt against the prepared base. It never
selects a newer build or regenerates the approved notes. Changed state fails and
requires fresh preparation and approval. Approval is authorization for this job's
specific operation; the JSON file alone is not an authorization credential.

One workflow concurrency group covers preparation, approval and mutation, with
cancellation disabled. GitHub may replace an older pending run with a newer one;
retained producer artifacts remain discoverable for manual dispatch. An approval
for one release cannot authorize another release. Branch/PR/manual producer builds
are ineligible. Store publishing is independent of GitHub Release/signing/WinGet.

The publisher copies current published metadata, changes package references,
release notes and publication settings, uploads the exact validated bundle inside
the API-required ZIP, and commits for immediate publication after certification.
Descriptions, screenshots, pricing, availability and other metadata are preserved.
English notes fill every existing locale, including German and note overrides;
listing descriptions stay localized. Metadata is checked again before upload and
commit. This preapproval summary covers the release and notes; the live metadata
snapshot is fetched and preserved after approval because it needs protected secrets.

Notes come from tagged `.github/release-notes.md`, generated by `make release`
from `todos.md`. The formatter removes Markdown, Internal entries and explicitly
Linux/macOS-only changes; mixed/unmarked entries stay. It keeps complete entries
within 1,500 UTF-16 units, a version and compare link, using a neutral version/link
note if no entry fits. Skipped release notes are included back to the recorded
published Store version. Initially that base is the confirmed 1.0.2 release;
later bases come from published receipts. Missing note predecessors fail visibly.

## Certification, waiting releases and recovery

There is **no scheduled Store polling**. GitHub withholds environment secrets until
the protected job is approved, including read-only jobs; a periodic check would
therefore create periodic approval requests. Credentials stay solely in this
protected environment. See [GitHub's environment secret and review rules](https://docs.github.com/en/actions/reference/workflows-and-actions/deployments-and-environments).

After the approved commit, Microsoft continues certification and publication
without a running workflow. `CommitStarted`, `Certification` and `Publishing` mean
processing; only `Published` confirms completion. Inspect Partner Center or
manually dispatch `check`/`reconcile` when status is needed, then approve that job.
There are no automatic post-job status updates or certification-failure notices
from this workflow. A manual observation reports failures and unexpected holds.

`reconcile` is limited to its prepared receipt: it may resume an interrupted upload
or commit, observe processing, or record completion/failure. It always stops after
that release. If another build completed during certification, reconcile the active
receipt first, then dispatch `submit` to prepare the requested/newest waiting
release and approve its new summary. An active receipt blocks preparing a different
submission, even if Microsoft has finished but completion has not yet been recorded.
No extra tag or rebuild is needed while the original artifact remains available.

From Actions -> Microsoft Store publisher -> Run workflow on `main`:

| Mode | Effect |
| --- | --- |
| `check` | Approved credential access; reads published/pending state and diagnoses a recorded pending draft, no Store or receipt writes. |
| `preview` | Approved credential access; validates a release and prints live metadata/notes, no writes. It does not authorize a later submission. |
| `submit` | Automatically prepares a release before approval, then submits only that exact release. |
| `reconcile` | Prepares the current receipt before approval, then resumes/observes only that receipt. |

The optional tag must be canonical `vMAJOR.MINOR.PATCH`. Empty `submit` selects the
newest eligible release during preparation. Empty `reconcile` selects the durable
receipt; an explicit different tag is rejected. Results appear in the summary and
90-day diagnostic artifacts. A successful read-only check proves read access only;
it does not prove upload/submission permission or that a pending release is live.

When a draft is still `PendingCommit` and matches the saved receipt, `check` adds
`pending_validation`. This compares the draft with the receipt's original published
base and with the update prepared from that base and the frozen release notes.
`changes_from_base` and `changes_from_prepared` contain field paths only, with a
32-difference limit; metadata values and upload URLs are omitted. The accompanying
booleans report whether metadata and packages match the receipt and whether the
original base still produces its recorded metadata hash. Use this report to
investigate a metadata mismatch before changing validation or resuming the draft.

The durable receipt journal uses GitHub deployments with task
`picfetch-store-receipt`, environment `microsoft-store`, ref `main` and
`auto_merge: false`. It binds tag/commit, run/attempt, artifact ID, hashes, frozen
notes, base submission/version, submission ID, metadata hash and phase. Receipts
survive runner replacement and artifact expiry; unchanged polls append no copies.
Keep this history. Preparation reads receipts with `deployments: read`; only the
approved job has `deployments: write`. No git commits or mutable release assets
are used as state.

Creation intent is saved before Microsoft's non-idempotent create call. A lost
create response is an **ambiguous creation** conflict; no ownership/idempotency
marker lets the publisher safely adopt or delete that draft. Preserve the draft
and receipt and inspect the outcome. There is no automatic destructive reset.
Interrupted metadata updates/uploads resume using the recorded submission and
original artifact. Upload URLs are refreshed but never persisted. Commit ambiguity
is observed by submission ID; an ambiguous `PendingCommit` is never blindly
committed again. Unknown drafts, metadata drift, moved tags, invalid provenance,
expired artifacts and unknown statuses stop mutation. Rejected identical bits
cannot be submitted repeatedly; corrected higher versions need a new approval.

GET/transient upload retries are bounded, respect `Retry-After`, and support
cancellation. Tokens refresh on expiry or authorization failure. The command has
a 12-minute deadline inside a 15-minute job. If interrupted, dispatch a fresh
`reconcile` operation and approve its frozen recovery summary. Rerunning only the
old protected job retains its old manifest: if the receipt changed, it fails closed
and requires fresh preparation. No new candidate is selected by a rerun.

Microsoft advises keeping API-created submissions under API management; manual
edits can make them uncommittable. See [submission management guidance](https://learn.microsoft.com/en-us/windows/uwp/monetize/manage-app-submissions).
To pause future work, disable the publisher workflow in Actions. Already committed
Microsoft certification is independent of that setting. Preserve pending drafts
and journal entries while investigating.

## Account setup and live validation

The maintainer confirmed PicFetch 1.0.2 is already published, a tenant is linked,
and a dedicated **PicFetch Store Publisher** Entra application is configured with
**Developer**. Microsoft's [current role table](https://learn.microsoft.com/en-us/windows/apps/publish/partner-center/assign-roles-to-account-users)
says Developer can upload packages and submit apps, including roles assigned to
Entra applications. The [submission API setup guide](https://learn.microsoft.com/en-us/windows/uwp/monetize/create-and-manage-submissions-using-windows-store-services)
still says Manager. Developer is the deliberately chosen narrower role; do not
elevate it silently. A successful read-only check will not settle submission
permission. The next authorized ordinary update supplies that evidence; report any
permission denial for a deliberate decision about permissions.

Read-only GitHub API inspection on 2026-09-08 verified the existing environment:
only branch `main`, Required reviewers with exactly `frathe`, Prevent self-review
off, administrator bypass off, no wait timer or custom rules. The workflow and
setup script validate those settings without changing them. These secret names
exist in the environment; their values were not retrieved:

| Secret | Source |
| --- | --- |
| `MSSTORE_TENANT_ID` | Linked Entra directory's Tenant ID |
| `MSSTORE_CLIENT_ID` | Publisher application's Client ID |
| `MSSTORE_CLIENT_SECRET` | Application key value |

The user confirmed revoking the old key, generating a replacement, updating the
GitHub secret and closing the key display. For later rotation, record expiry in the
normal credential manager and enter replacement values directly into GitHub
Environment Secrets. Never capture values in chat, local files, shell commands,
logs, screenshots or this repository. Do not duplicate them to another environment.
The optional `.scratch/microsoft-store-updates/configure-access.sh` now guides
human-only dashboard steps and verifies metadata/secret names; it neither accepts
credential values nor runs a local Microsoft check. Setup is already complete.

The publisher workflow was absent from GitHub's installed workflows at inspection.
No existing trusted workflow exposes a read-only Microsoft credential test. After
this implementation is landed on `main`, dispatch `check`, approve its protected
job and confirm product `9P0DM0KTH01K`, published version and pending state from the
sanitized summary. Actual credential validity, API read access and submission
permission remain unverified. No workflow dispatch, Store submission or live
protection change was performed in this local amendment. Do not retag or resubmit
1.0.2 as a test; observe the next ordinary approved stable release through Published.

```sh
go test -race ./scripts/storepublish
go test ./scripts/msixstage -run 'Test(MicrosoftStore|StoreWorkflow)'
```

Fake-service tests cover the local contract. GitHub approval enforcement, Windows
SDK/WACK and live Microsoft acceptance still need their own real-service evidence.

## Versioning

MSIX requires a four-part numeric version with a non-zero first component, and
the Store reserves the fourth component. Starting with PicFetch 1.0.0, the
three-part public `Version` in `FyneApp.toml` is therefore also the Store
version, with a trailing zero added for MSIX: for example, PicFetch `1.0.0`
becomes `1.0.0.0` and PicFetch `1.0.1` becomes `1.0.1.0`. The separate Fyne
`Build` counter remains internal packaging metadata and does not affect the
public version in either release channel.

MakeAppx currently assigns the **outer bundle** a separate timestamp version. The
accepted 1.0.2 artifact from producer run `33983485123` has outer version
`2026.905.1829.0` and inner versions `1.0.2.0`. The publisher records both versions,
checks that both advance, and binds future API-reported versions to the recorded
submission. Its bootstrap mapping recognizes only that accepted bundle or inner
`1.0.2.0`. An unfamiliar published version without a matching receipt fails for
inspection instead of being silently mistaken for a newer application release.
No bundle versioning or rebuilding of the accepted artifact is introduced here.

Microsoft's [package resource](https://learn.microsoft.com/en-us/windows/uwp/monetize/manage-app-submissions#application-package-resource)
does not guarantee an inner-version representation for bundles;
[StoreBroker's bundle inspection](https://github.com/microsoft/StoreBroker/blob/master/StoreBroker/PackageTool.ps1)
also treats the outer and inner versions separately. The initial live `check`
therefore remains part of setup.

Store packaging deliberately rejects versions below 1.0.0, prerelease labels,
and components outside MSIX's 0..65535 range. Version 1.0.2 is already live; updates must advance beyond the published version.

## Original listing reference

These settings describe the accepted listing and are preserved by update automation.

1. Under Properties, select that PicFetch accesses or transmits personal
   information and set the privacy-policy URL to
   `https://github.com/frathe/picfetch/blob/main/PRIVACY.md`. The declaration
   covers the user-initiated OpenStreetMap requests documented in that policy.
2. Upload the bundle under Submission -> Packages and leave only Windows 10/11
   Desktop enabled.
3. Explain the detected `runFullTrust` restricted capability in Submission
   options: PicFetch is a native Fyne/Win32 desktop image viewer. It needs normal
   desktop process access to open user-selected files and folders, save/export
   images, copy files and pixels, move selected files to the Recycle Bin, and set
   wallpaper. It does not elevate, install services, or run background tasks.
4. Add English and German listings using
   `packaging/microsoft-store/listing.md` and its referenced assets.
5. Initial manual submission is complete. Subsequent stable updates use the
   approved API path above.

The package identity values in `scripts/msixstage` are case-sensitive and come
from Partner Center. If the reserved product changes, update them together with
this document and the plan before building another bundle.
